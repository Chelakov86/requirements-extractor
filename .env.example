# Dev default — SQLite, no Docker needed
DATABASE_URL=sqlite:///./reqext.db
SECRET_KEY=change-me-in-production
GEMINI_API_KEY=your-gemini-api-key-here

# Production — switch to PostgreSQL and use docker compose
# DATABASE_URL=postgresql://reqext:reqext@db:5432/reqext
# POSTGRES_USER=reqext
# POSTGRES_PASSWORD=reqext
# POSTGRES_DB=reqext
