# Sample Markdown Document

This is a **sample markdown** file used for testing the file parser service.

## Requirements

- The system _shall_ parse Markdown files.
- It must handle more than 100 characters of content.
- Parsing must return the raw text without rendering.

## Notes

This fixture ensures that plain-text extraction from `.md` files works correctly.
